---
- name: Setup NGINX Server
  host: localhost
  remote_user: user1
  tasks:
    - name: Install nginx
      apt:
        name: nginx
        state: latest
    - name: start nginx
      service:
        name: nginx
        state: restarted
